# Spec NNNN: <short title>

- **Status:** proposed | approved | shipped
- **Closes issue:** `issues/open/NNNN-<slug>.md` (or "none — new feature")

## Why

One paragraph: what problem this solves for someone using the app.

## What changes

The user-visible behaviour after this ships. Be concrete enough that the result
can be checked by looking at the screen.

- ...
- ...

## Out of scope

What this step deliberately does *not* do, so the change stays small.

- ...

## Data

Any change to the `localStorage` shape, and how existing stored data is handled.

## Acceptance checks

Things the human can do to verify it works.

1. ...
2. ...
